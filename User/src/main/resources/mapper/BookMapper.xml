<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blind.user.repository.BookMapper">
    <insert id="addBook">
        insert into books(book_name,content,pinyin) values (#{bookName},#{content},#{pinyin})
    </insert>
</mapper>